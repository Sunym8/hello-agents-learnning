# Direct dependencies used by this project (version-locked for reproducibility)
# Install with: pip install -r requirements.txt

requests==2.34.2
tavily-python==0.8.1
openai==3.8.0
python-dotenv==1.2.3
